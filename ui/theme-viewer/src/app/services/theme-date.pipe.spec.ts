import { ThemeDatePipe } from './theme-date.pipe';

describe('ThemeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
